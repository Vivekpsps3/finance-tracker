---
phase: 01-freeze-the-schema
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - OVERVIEW-01
  - NW-01
  - NW-02
  - NW-03
  - ACT-01
  - ACT-02
  - ACT-03
  - CF-01
  - CF-02
  - CF-03
  - PLAN-01
  - PLAN-02
  - PLAN-03
  - AUTH-01
  - VAULT-01
  - AUTH-02

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - PROJECT.md names the four write epics NW, ACT, CF, PLAN and states Overview is a read-only cockpit
    - REQUIREMENTS.md lists every Phase 1 story with Plane, Writes, Must not, and Accept
    - ROADMAP.md maps the sixteen validated stories to Phase 1 as existing and maps OVERVIEW-02, IA-01, IA-02 to Phase 2
    - Phase 2 completion marks on ROADMAP.md stay checked
    - No new product story IDs are invented
  artifacts:
    - path: .planning/PROJECT.md
      provides: Four write epics plus Overview cockpit
      contains: "**NW**"
    - path: .planning/REQUIREMENTS.md
      provides: Lean schema for every Phase 1 story
      contains: "**OVERVIEW-01**"
    - path: .planning/ROADMAP.md
      provides: Phase 1 existing-story map and Phase 2 IA story map
      contains: "OVERVIEW-01"
  key_links:
    - from: .planning/PROJECT.md
      to: docs/superpowers/specs/2026-08-30-organization-and-user-story-schema.md
      via: Epic names copied from the spec write-epics table
      pattern: "\\*\\*NW\\*\\*"
    - from: .planning/REQUIREMENTS.md
      to: .planning/ROADMAP.md
      via: Sixteen Phase 1 IDs listed as existing; IA stories stay on Phase 2
      pattern: "VAULT-01"
---

<objective>
Freeze the organization and lean user-story schema into GSD planning docs.

Purpose: Later phases and agents read one locked schema. The sixteen already-shipped stories stay documented as existing; this phase invents no product work.
Output: PROJECT.md states the four write epics and Overview cockpit; REQUIREMENTS.md has the lean schema on every Phase 1 story; ROADMAP.md keeps the existing-vs-Phase-2 map; Phase 2 IA completion marks stay intact.
</objective>

<execution_context>
@/home/vivek/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/vivek/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-freeze-the-schema/01-CONTEXT.md
@.planning/phases/01-freeze-the-schema/01-PATTERNS.md
@docs/superpowers/specs/2026-08-30-organization-and-user-story-schema.md

Discretion (CONTEXT.md has no D-XX rows; honor these):
- Planning files already exist. Verify, then fill gaps. Do not recreate from empty GSD templates.
- Phase 2 IA already shipped on disk. Do not uncheck Phase 2, do not move OVERVIEW-02 / IA-01 / IA-02 out of Validated, do not reset STATE.md to 0%.
- 01-PATTERNS.md is the analog for section order and the epic table. Its "restore Phase 1 freeze view" checkbox instructions are superseded by the Phase 2-already-shipped rule above.
- Touch only `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, and `.planning/ROADMAP.md`. Leave application code, the spec, docs/ARCHITECTURE.md, docs/DATA_MODEL.md, and STATE.md frontmatter alone.
</context>

<tasks>

<task type="tracer">
  <name>End-to-end schema freeze — four epics through lean OVERVIEW-01 to the roadmap map</name>
  <precondition>docs/superpowers/specs/2026-08-30-organization-and-user-story-schema.md exists and is non-empty</precondition>
  <files>.planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>docs/superpowers/specs/2026-08-30-organization-and-user-story-schema.md, .planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/phases/01-freeze-the-schema/01-PATTERNS.md, .planning/STATE.md</read_first>
  <action>
Wire one path through the three planning files so a later agent can read the frozen schema without guessing.

On PROJECT.md: if the write-epics table from 01-PATTERNS.md Shared Patterns (four rows NW, ACT, CF, PLAN with Plane / May write / Must not) is absent, insert that table plus the spec Read cockpit sentence (Overview at `/` is a read-only composition and writes none) immediately after the Core Value section. Keep Validated items that already list the four epics and the shipped IA stories. Do not add a Business Context section. Do not copy ARCHITECTURE.md headers.

On REQUIREMENTS.md: confirm OVERVIEW-01 has the lean fields Plane, Writes, Must not, Accept. Confirm the other fifteen Phase 1 IDs exist with those same four field labels. Do not add stories. Do not reformat every block if the fields are already present (one-line `Plane: … · Writes: …` is acceptable).

On ROADMAP.md: confirm Phase 1 Requirements lists the sixteen IDs above and Phase 2 Requirements lists OVERVIEW-02, IA-01, IA-02. Leave the Phase 2 header checkbox and the Phase 2 plan checkbox checked. If Phase 1 still says `**Plans**: inline` or lists `01-01:` without the `01-01-PLAN.md` filename, change only the Phase 1 Plans count to `1 plan` and the Phase 1 plan bullet to `01-01-PLAN.md — Verify and freeze planning docs against the spec`. Do not edit the Phase 2 block.

Do not edit STATE.md. Do not edit application source.
  </action>
  <verify>
    <automated>python3 -c "from pathlib import Path; req=Path('.planning/REQUIREMENTS.md').read_text(); proj=Path('.planning/PROJECT.md').read_text(); road=Path('.planning/ROADMAP.md').read_text(); ids='OVERVIEW-01 NW-01 NW-02 NW-03 ACT-01 ACT-02 ACT-03 CF-01 CF-02 CF-03 PLAN-01 PLAN-02 PLAN-03 AUTH-01 VAULT-01 AUTH-02'.split(); missing=[i for i in ids if f'**{i}**' not in req];
assert not missing, missing; 
assert all(req.count(t)>=16 for t in ['Plane:','Writes:','Must not:','Accept:']);
assert all(e in proj for e in ['NW','ACT','CF','PLAN']) and 'Overview' in proj;
assert 'OVERVIEW-02' in road and 'IA-01' in road and 'IA-02' in road;
assert '[x] **Phase 2:' in road; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - .planning/PROJECT.md contains NW, ACT, CF, PLAN, and Overview
    - .planning/REQUIREMENTS.md contains **OVERVIEW-01** and at least sixteen of each lean label Plane:, Writes:, Must not:, Accept:
    - .planning/ROADMAP.md contains OVERVIEW-02, IA-01, and IA-02
    - .planning/ROADMAP.md still contains [x] **Phase 2:
  </acceptance_criteria>
  <done>One path works: PROJECT names the four write epics and Overview cockpit, REQUIREMENTS carries lean OVERVIEW-01 plus the other fifteen Phase 1 IDs, ROADMAP maps existing vs Phase 2 stories, and Phase 2 stays marked complete.</done>
</task>

<task type="auto">
  <name>Fill remaining lean-schema fields from the spec</name>
  <files>.planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>docs/superpowers/specs/2026-08-30-organization-and-user-story-schema.md, .planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</read_first>
  <action>
Fill only the gaps below. Keep every existing story ID. Keep OVERVIEW-02, IA-01, and IA-02 checked. Keep Phase 2 ROADMAP checkboxes checked.

REQUIREMENTS.md Accept bullets — add the missing second check when it is absent (spec v1 Stories). Leave wording if it is already there:
- NW-03: values stay encrypted
- CF-01: feeds cashflow summary
- CF-02: not as a required transaction
- AUTH-01: legacy password path is migration-only
- AUTH-02: lost passphrase means lost data

PROJECT.md Key Decisions: set Outcome on the four existing rows (one epic per write plane; Overview is read-only; lean stories; two-phase milestone) from `— Pending` to `✓ Good`. Update the footer date line to 2026-08-30 after schema freeze. Do not add rows. Do not invent a fifth write epic.

ROADMAP.md: if Task 1 already set Phase 1 to `**Plans:** 1 plan` and `01-01-PLAN.md`, leave it. Still do not touch the Phase 2 section.

Do not add SimpleFIN, Plaid, household sharing, tax vault, net-worth history, or new finance features to Active. Do not edit STATE.md or application source.
  </action>
  <verify>
    <automated>python3 -c "from pathlib import Path; req=Path('.planning/REQUIREMENTS.md').read_text().lower(); proj=Path('.planning/PROJECT.md').read_text(); road=Path('.planning/ROADMAP.md').read_text();
assert 'values stay encrypted' in req; assert 'feeds cashflow summary' in req; assert 'not as a required transaction' in req; assert 'legacy password path is migration-only' in req; assert 'lost passphrase means lost data' in req;
assert proj.count('✓ Good')>=4; assert '— Pending' not in proj.split('## Key Decisions',1)[-1];
assert '01-01-PLAN.md' in road and '[x] **Phase 2:' in road; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - .planning/REQUIREMENTS.md contains values stay encrypted, feeds cashflow summary, not as a required transaction, legacy password path is migration-only, and lost passphrase means lost data
    - .planning/PROJECT.md Key Decisions table has at least four ✓ Good outcomes
    - .planning/ROADMAP.md contains 01-01-PLAN.md
    - .planning/ROADMAP.md still contains [x] **Phase 2:
  </acceptance_criteria>
  <done>Spec Accept checks that were missing are present; Key Decisions are marked Good; Phase 1 points at 01-01-PLAN.md; Phase 2 completion is unchanged.</done>
</task>

</tasks>

<artifacts>
- `.planning/PROJECT.md` — four write epics + Overview cockpit + decided Key Decisions
- `.planning/REQUIREMENTS.md` — lean schema on all sixteen Phase 1 stories
- `.planning/ROADMAP.md` — existing-story map on Phase 1; IA stories on Phase 2; Phase 2 still complete
- `.planning/phases/01-freeze-the-schema/COVERAGE.md` — no external API (already written)
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none (docs only) | This phase edits GSD markdown. No runtime input, no HTTP, no vault, no browser. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | `.planning/ROADMAP.md` Phase 2 marks | medium | mitigate | Task verify asserts `[x] **Phase 2:` remains; executor must not uncheck shipped IA |
| T-01-02 | Information Disclosure | `.planning/*.md` story text | low | accept | Planning docs already live in the repo; no new secrets |
| T-01-03 | Elevation of Privilege | AUTH-02 / admin copy | low | accept | Docs only restates existing `/admin/users` cannot recover vaults |
| T-01-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this phase |
</threat_model>

<verification>
python3 -c "from pathlib import Path; req=Path('.planning/REQUIREMENTS.md').read_text(); proj=Path('.planning/PROJECT.md').read_text(); road=Path('.planning/ROADMAP.md').read_text(); ids='OVERVIEW-01 NW-01 NW-02 NW-03 ACT-01 ACT-02 ACT-03 CF-01 CF-02 CF-03 PLAN-01 PLAN-02 PLAN-03 AUTH-01 VAULT-01 AUTH-02'.split(); assert all(f'**{i}**' in req for i in ids); assert all(e in proj for e in ['NW','ACT','CF','PLAN']); assert 'Overview' in proj; assert '[x] **Phase 2:' in road; print('phase-ok')"
</verification>

<success_criteria>
- PROJECT.md states the four write epics and Overview cockpit
- REQUIREMENTS.md lists every Phase 1 story with the lean schema
- ROADMAP.md maps validated stories as existing and IA stories to Phase 2
- Phase 2 completion marks are still checked
- No application code changed
</success_criteria>

<output>
Create `.planning/phases/01-freeze-the-schema/01-01-SUMMARY.md` when done
</output>
