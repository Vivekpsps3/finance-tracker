---
phase: 02-align-app-ia
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/core/layout/main-layout.component.ts
  - frontend/src/app/core/layout/main-layout.component.html
  - frontend/src/app/shared/ui/ui-source-badge/ui-source-badge.component.ts
  - frontend/src/app/dashboard/dashboard.component.html
  - frontend/src/app/transactions/transactions.component.html
autonomous: true
requirements:
  - OVERVIEW-02
  - IA-01
  - IA-02

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - A user can tell which plane they are in from the chrome
    - Five nav groups remain Overview, Net Worth, Cashflow, Activity, Planning and each group tooltip names the plane and the must-not (D-01, D-02)
    - Overview chrome states it is a read-only picture and sends edits to other planes (D-03, OVERVIEW-02)
    - Tutorial map order is Overview, Net Worth, Cashflow, Activity, Planning and steps are grouped by plane not by table (D-05, D-06)
    - Badge labels are NW, CF, ACT+CF, PLAN and no net-worth control uses the ACT+CF or PLAN kind (D-07, D-08, D-09, D-10, D-11, IA-02)
  artifacts:
    - path: frontend/src/app/core/layout/main-layout.component.ts
      provides: Five nav groups with plane plus must-not tooltips
      contains: "label: 'Overview'"
    - path: frontend/src/app/core/layout/main-layout.component.html
      provides: Tutorial map and plane-ordered steps
      contains: "tutorial-map"
    - path: frontend/src/app/shared/ui/ui-source-badge/ui-source-badge.component.ts
      provides: Plane labels on source kinds
      contains: "observed: 'NW'"
    - path: frontend/src/app/dashboard/dashboard.component.html
      provides: Read-only Overview subtitle and NW hero badge
      contains: "Read-only picture"
    - path: frontend/src/app/transactions/transactions.component.html
      provides: Activity month totals named as period Activity not Net Worth
      contains: "month-total__label"
  key_links:
    - from: frontend/src/app/core/layout/main-layout.component.html
      to: frontend/src/app/core/layout/main-layout.component.ts
      via: Nav tabs bind group.tooltip from navGroups
      pattern: "group.tooltip"
    - from: frontend/src/app/dashboard/dashboard.component.html
      to: frontend/src/app/shared/ui/ui-source-badge/ui-source-badge.component.ts
      via: Overview hero uses the NW kind on the current net-worth figure
      pattern: "kind=\"observed\""
    - from: frontend/src/app/transactions/transactions.component.html
      to: frontend/src/app/shared/ui/ui-source-badge/ui-source-badge.component.ts
      via: Month totals use the ACT+CF period kind
      pattern: "kind=\"combined\""
---

<objective>
Verify shipped chrome against the IA table and patch only mapping gaps so a user can tell which plane they are in (D-01 through D-11).

Purpose: Commit 68a9d8c already named planes in nav, titles, tutorial, and badge labels. This plan formalizes that work and fixes leftover ledger totals that still use the Net Worth kind.
Output: Nav, Overview subtitle, tutorial, and source-badge call sites match OVERVIEW-02, IA-01, and IA-02. No new routes, tokens, or layout.
</objective>

<execution_context>
@/home/vivek/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/vivek/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-align-app-ia/02-CONTEXT.md
@.planning/phases/02-align-app-ia/02-UI-SPEC.md
@docs/superpowers/specs/2026-08-30-organization-and-user-story-schema.md
@frontend/src/app/core/layout/main-layout.component.ts
@frontend/src/app/core/layout/main-layout.component.html
@frontend/src/app/shared/ui/ui-source-badge/ui-source-badge.component.ts
@frontend/src/app/dashboard/dashboard.component.html
@frontend/src/app/transactions/transactions.component.html

Locked decisions (CONTEXT.md has no D-XX rows; treat these as D-01..D-11):
- D-01 Keep the five existing nav groups
- D-02 Each group tooltip names the plane and the must-not
- D-03 Overview is a read-only picture; edit other planes on their own pages
- D-04 Page subtitles name the plane and the must-not
- D-05 Tutorial map order: Overview, Net Worth, Cashflow, Activity, Planning
- D-06 Tutorial steps stay ordered by plane, not by table
- D-07 observed maps to NW
- D-08 scheduled maps to CF
- D-09 combined maps to ACT+CF
- D-10 scenario maps to PLAN
- D-11 Never put the ACT+CF or PLAN kind on a net-worth figure

Discretion: chrome already shipped in 68a9d8c. Verify first. Write only on mismatch. No layout, token, or route changes.

<interfaces>
From frontend/src/app/core/layout/main-layout.component.ts:
- NavItem: path, label, shortLabel, icon, exact, tooltip
- NavGroup: label, shortLabel, icon, tooltip, optional adminOnly, items
- navGroups order must stay Overview, Net Worth, Cashflow, Activity, Planning

From frontend/src/app/shared/ui/ui-source-badge/ui-source-badge.component.ts:
- UiSourceKind: observed | scheduled | combined | scenario
- LABELS: observed NW, scheduled CF, combined ACT+CF, scenario PLAN
- DEFAULT_TITLES already name the plane and the must-not
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>End-to-end plane chrome — nav group, Overview subtitle, badge labels</name>
  <files>frontend/src/app/core/layout/main-layout.component.ts, frontend/src/app/dashboard/dashboard.component.html, frontend/src/app/shared/ui/ui-source-badge/ui-source-badge.component.ts</files>
  <read_first>frontend/src/app/core/layout/main-layout.component.ts, frontend/src/app/dashboard/dashboard.component.html, frontend/src/app/shared/ui/ui-source-badge/ui-source-badge.component.ts, .planning/phases/02-align-app-ia/02-CONTEXT.md, .planning/phases/02-align-app-ia/02-UI-SPEC.md</read_first>
  <action>
Wire one path a signed-in user sees on load: top nav plane, Overview header, Net Worth badge on the hero. Per D-01 keep exactly these five navGroups labels in this order: Overview, Net Worth, Cashflow, Activity, Planning. Do not add or remove routes or groups. Per D-02 each group's tooltip must include that group's plane name and a must-not (Overview does not edit other planes; Net Worth is not a transaction rollup; Cashflow / Activity do not change net worth; Planning does not mutate assets, holdings, or transactions). Per D-03 and OVERVIEW-02 the Overview ui-page-header title stays Overview and the subtitle must say it is a read-only picture of current Net Worth then period Activity and Cashflow, with edits on those planes' own pages. Per D-07 through D-10, LABELS in ui-source-badge must stay observed to NW, scheduled to CF, combined to ACT+CF, scenario to PLAN. DEFAULT_TITLES must name the same planes. If those three layers already match, make no edits. Do not restyle tokens, type, or layout. Do not edit tutorial HTML or transaction badges here.
  </action>
  <verify>
    <automated>python3 -c "from pathlib import Path; import re; ts=Path('frontend/src/app/core/layout/main-layout.component.ts').read_text(); dash=Path('frontend/src/app/dashboard/dashboard.component.html').read_text(); badge=Path('frontend/src/app/shared/ui/ui-source-badge/ui-source-badge.component.ts').read_text(); block=ts.split('readonly navGroups',1)[1].split('];',1)[0]; planes=['Overview','Net Worth','Cashflow','Activity','Planning']; idxs=[block.index(\"label: '%s'\" % p) for p in planes]; assert idxs==sorted(idxs), idxs; tips=re.findall(r\"tooltip: '([^']+)',\\n\\s+items:\", block); assert len(tips)==5 and all(p in t for p,t in zip(planes, tips)), tips; assert all(re.search(r'[Nn]ot |[Dd]oes not ', t) for t in tips), tips; assert 'title=\"Overview\"' in dash and 'Read-only picture' in dash and 'Edit those planes' in dash; assert \"observed: 'NW'\" in badge and \"scheduled: 'CF'\" in badge and \"combined: 'ACT+CF'\" in badge and \"scenario: 'PLAN'\" in badge; print('OK')"</automated>
    <human-check>Open Overview: the five nav tabs read Overview / Net Worth / Cashflow / Activity / Planning, hover names the plane and the must-not, the page title is Overview, and the hero badge reads NW.</human-check>
  </verify>
  <acceptance_criteria>
    - navGroups group labels are Overview, Net Worth, Cashflow, Activity, Planning in that order (D-01)
    - Each of the five group tooltips contains its plane name and a must-not (D-02)
    - dashboard.component.html Overview subtitle contains Read-only picture and Edit those planes (D-03, OVERVIEW-02)
    - ui-source-badge LABELS map observed to NW, scheduled to CF, combined to ACT+CF, scenario to PLAN (D-07, D-08, D-09, D-10, IA-02)
  </acceptance_criteria>
  <done>One path works: nav names the five planes, Overview says it is a read-only picture, and badge labels name NW / CF / ACT+CF / PLAN.</done>
</task>

<task type="auto">
  <name>Verify tutorial map and steps stay plane-ordered</name>
  <files>frontend/src/app/core/layout/main-layout.component.html</files>
  <read_first>frontend/src/app/core/layout/main-layout.component.html, .planning/phases/02-align-app-ia/02-CONTEXT.md</read_first>
  <action>
Per D-05 the tutorial-map column labels must be Overview, then Net Worth, then Cashflow, then Activity, then Planning. Per D-06 every tutorial-step__group text must be one of those plane names, never a table name (Holdings, Transactions, Income, Bills, Subscriptions) as the step group. Steps may stay Net Worth, Net Worth, Activity, Cashflow, Planning; do not split a plane into extra top-level groups. Per OVERVIEW-02 the tutorial intro must say Overview is a read-only picture and that other planes are edited on their own pages. Per D-04, if you open the finance page headers while here, each subtitle must already name its plane and must-not; patch a subtitle only when that sentence is missing, and do not restyle the header. If the tutorial already matches, make no edits. Do not add routes or restyle the overlay.
  </action>
  <verify>
    <automated>python3 -c "from pathlib import Path; import re; html=Path('frontend/src/app/core/layout/main-layout.component.html').read_text(); mmap=html.split('tutorial-map',1)[1].split('tutorial-grid',1)[0]; maps=re.findall(r'<span>([^<]+)</span>', mmap); assert maps==['Overview','Net Worth','Cashflow','Activity','Planning'], maps; groups=re.findall(r'tutorial-step__group\">([^<]+)', html); allowed={'Overview','Net Worth','Cashflow','Activity','Planning'}; assert groups and all(g in allowed for g in groups), groups; assert 'read-only picture' in html.lower(); pages={'frontend/src/app/dashboard/dashboard.component.html':'Read-only picture','frontend/src/app/assets-liabilities/assets-liabilities.component.html':'Net Worth','frontend/src/app/portfolio/portfolio.component.html':'Net Worth','frontend/src/app/income/income.component.html':'Cashflow','frontend/src/app/fixed-expenses/fixed-expenses.component.html':'Cashflow','frontend/src/app/subscriptions/subscriptions.component.html':'Cashflow','frontend/src/app/transactions/transactions.component.html':'Activity','frontend/src/app/calendar/calendar.component.html':'Activity','frontend/src/app/planning/planning.component.html':'Planning','frontend/src/app/investment-insights/investment-insights.component.html':'Planning','frontend/src/app/stock-lab/stock-lab.component.html':'Planning'};
[(_p, Path(_p).read_text()) for _p in pages];
assert all(v in Path(p).read_text() for p,v in pages.items()); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - tutorial-map spans are Overview, Net Worth, Cashflow, Activity, Planning (D-05, IA-01)
    - every tutorial-step__group is a plane name, not a table name (D-06)
    - tutorial intro contains read-only picture (OVERVIEW-02)
    - listed finance page headers still contain their plane name (D-04)
  </acceptance_criteria>
  <done>Tutorial map follows the locked order and steps are grouped by plane; Overview still reads as a picture, not an editor.</done>
</task>

<task type="auto">
  <name>Map leftover ledger badges off the Net Worth kind</name>
  <files>frontend/src/app/dashboard/dashboard.component.html, frontend/src/app/transactions/transactions.component.html</files>
  <read_first>frontend/src/app/dashboard/dashboard.component.html, frontend/src/app/transactions/transactions.component.html, frontend/src/app/planning/planning.component.html, frontend/src/app/shared/ui/ui-source-badge/ui-source-badge.component.ts</read_first>
  <action>
Per D-11 keep the Overview net-worth-actions badge and any Planning label that reads Current net worth or Portfolio exposure on the NW kind. Per D-07 that kind means Net Worth only. Per D-09 and IA-02, period Activity figures must use the ACT+CF kind: the three month-total__label badges on the transactions page, the four insight-tile period totals on Overview, and the cashflow-strip Income and Expenses badges (those values are ledger Activity, not the balance sheet). Per D-08 keep Job net and Bills + subs on the CF kind. If the Overview cashflow heading still says Observed + scheduled, change that heading and the overlap note so they name Activity and Cashflow, not Net Worth. Do not add a fifth source kind. Do not restyle badge CSS. Do not change evidence-card mappings on Planning or Stock Lab unless a net-worth figure there uses the ACT+CF or PLAN kind.
  </action>
  <verify>
    <automated>python3 -c "from pathlib import Path; dash=Path('frontend/src/app/dashboard/dashboard.component.html').read_text(); tx=Path('frontend/src/app/transactions/transactions.component.html').read_text(); plan=Path('frontend/src/app/planning/planning.component.html').read_text(); nw=dash.split('net-worth-actions',1)[1].split('</div>',1)[0]; assert 'kind=\"observed\"' in nw; strip=dash.split('Cashflow source breakdown',1)[1].split('@if',1)[0]; assert strip.count('kind=\"combined\"')==2 and strip.count('kind=\"scheduled\"')==2; tiles=dash.split('insight-tiles',1)[1].split('cashflow-strip',1)[0]; assert tiles.count('kind=\"combined\"')==4; assert tx.count('kind=\"combined\"')==3; assert 'kind=\"observed\"' in plan.split('Current net worth',1)[1].split('</span>',1)[0]; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - dashboard net-worth-actions contains the NW kind (D-11, IA-02)
    - dashboard cashflow-strip contains two ACT+CF kinds and two CF kinds (D-08, D-09)
    - transactions month-totals contains three ACT+CF kinds (D-09, IA-02)
    - planning Current net worth span stays on the NW kind (D-07, D-11)
  </acceptance_criteria>
  <done>Net-worth figures stay NW. Period Activity totals use ACT+CF. Scheduled Cashflow stays CF. PLAN remains only on speculative controls.</done>
</task>

</tasks>

<artifacts>
- frontend/src/app/core/layout/main-layout.component.ts — five plane nav groups
- frontend/src/app/core/layout/main-layout.component.html — tutorial map and plane-ordered steps
- frontend/src/app/shared/ui/ui-source-badge/ui-source-badge.component.ts — NW / CF / ACT+CF / PLAN labels
- frontend/src/app/dashboard/dashboard.component.html — read-only Overview plus correct hero and period badges
- frontend/src/app/transactions/transactions.component.html — month totals named as period Activity
- .planning/phases/02-align-app-ia/COVERAGE.md — no external API
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none (copy only) | This phase edits nav tooltips, page subtitles, tutorial copy, and source-badge kinds. No new HTTP, vault, or auth surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | Overview net-worth hero badge | medium | mitigate | Task 3 keeps the NW kind on net-worth-actions so period ACT+CF badges cannot be read as the balance sheet (D-11) |
| T-02-02 | Information Disclosure | Tutorial and subtitle copy | low | accept | Copy restates existing plane rules already in the repo; no new secrets |
| T-02-03 | Elevation of Privilege | Account menu copy | low | accept | Lock vault / logout / admin links are unchanged support chrome, not money planes |
| T-02-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this phase |
</threat_model>

<verification>
python3 -c "from pathlib import Path; import re; ts=Path('frontend/src/app/core/layout/main-layout.component.ts').read_text(); html=Path('frontend/src/app/core/layout/main-layout.component.html').read_text(); badge=Path('frontend/src/app/shared/ui/ui-source-badge/ui-source-badge.component.ts').read_text(); dash=Path('frontend/src/app/dashboard/dashboard.component.html').read_text(); tx=Path('frontend/src/app/transactions/transactions.component.html').read_text(); block=ts.split('readonly navGroups',1)[1].split('];',1)[0]; planes=['Overview','Net Worth','Cashflow','Activity','Planning']; idxs=[block.index(\"label: '%s'\" % p) for p in planes]; assert idxs==sorted(idxs); tips=re.findall(r\"tooltip: '([^']+)',\\n\\s+items:\", block); assert len(tips)==5; maps=re.findall(r'<span>([^<]+)</span>', html.split('tutorial-map',1)[1].split('tutorial-grid',1)[0]); assert maps==planes; assert \"observed: 'NW'\" in badge and \"combined: 'ACT+CF'\" in badge; assert 'Read-only picture' in dash; assert 'kind=\"observed\"' in dash.split('net-worth-actions',1)[1].split('</div>',1)[0]; assert tx.count('kind=\"combined\"')==3; print('phase-ok')"
</verification>

<success_criteria>
- Nav groups and tooltips match the IA table (D-01, D-02, IA-01)
- Tutorial steps are ordered by plane, not by table (D-05, D-06)
- No net-worth control uses the ACT+CF or PLAN kind (D-11, IA-02)
- Overview chrome states it is a read-only picture (D-03, OVERVIEW-02)
- No new routes, tokens, or layout
</success_criteria>

<output>
Create `.planning/phases/02-align-app-ia/02-01-SUMMARY.md` when done
</output>
