# Visual System And Interface Consistency

## Current Findings

`STR-004` provides tokens and common controls. `VIS-001` and `PLAT-002` identify missing shared metric provenance, state patterns, and platform presentation review.

## Target State

Use tokenized surface hierarchy, tabular financial numerals, locale-aware currency/date formatting, source badges, shared metrics, fields, tables, loading/error/empty states, and documented exceptions for bespoke analytical surfaces. Support appearance and contrast modes without weakening readable dense data.

## Acceptance Criteria

`VIS-001` primary pages use shared source/state components. `PLAT-002` validates system appearance, contrast, transparency, font loading choice, and locale output. No bespoke surface obscures whether a value is observed, scheduled, or modeled.

## Verification

Perform token inventory, visual regression review, and target-browser appearance checks.
